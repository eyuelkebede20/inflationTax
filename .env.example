# Copy to .env (or set in Vercel Project Settings -> Environment Variables).
# Leave unset to run the app in anonymous-only (localStorage) mode.
#
# Only these two are needed by the frontend. The publishable key is safe to
# expose in the browser; it is bundled into the client build.
VITE_SUPABASE_URL=https://czfzdihavyjoiuvlclxq.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_ThojpX4jfcy3GY1jz8JkCA_-pcnVimt

# NEVER commit your service/secret key or database password. They are
# server-only credentials and are NOT used by this client app.
# SUPABASE_SECRET_KEY=...
# DATABASE_URL=...
